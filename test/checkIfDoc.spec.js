describe('#checkIfDoc', () => {
  it('should be able to capture files with an @doc pragma');
  it('should capture an array of sections from after an @doc pragma');
  it('should ignore files without an @doc pragma');
});
